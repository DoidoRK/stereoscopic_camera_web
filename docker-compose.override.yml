services:
  web:
    build:
      context: .
      target: dev

    command:
      - npm
      - run
      - dev
      - --
      - --host

    volumes:
      - .:/app
      - /app/node_modules

    ports:
      - "5173:5173"
